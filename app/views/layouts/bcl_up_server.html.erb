<!DOCTYPE html>
<html>
<head>
  <title>BCL-UP Server</title>
  <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css">
  <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.3.1/jquery.min.js"></script>
  <script src="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/js/bootstrap.min.js"></script>
  <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  <%= csrf_meta_tags %>
</head>

<body>

<section aria-label="Cornell" role="banner">
  <div id="skiptocontent"><a href="#maincontent">skip to main content</a></div>
  <div id="bc-header" class="container">
    <a class="header-link" href="https://bluecore.info/"><img src="<%= image_url('bcl_up_server/BCL-UP1.png') %>" class="header-logo"></a>
  </div>
</section>

<div class="container">
  <nav id="masthead" class="navbar navbar-inverse navbar-static-top" role="navigation">
    <div class="container-fluid flex center round " >
      <div class="navbar-header flex center" >
        <ul class="nav-menu flex center pad-0 m-0">
          <% BclUpServer.config.navmenu_presenter.leftmenu_items.each do |item| %>
            <li class='left-menu'><%= link_to item[:label], item[:url] %></li>
          <% end %>
        </ul>
      </div>
    </div>
  </nav>
</div>

<div id="content-wrapper" class="container" role="main">
  <a name="skip_to_content"></a>
  <% flash.each do |key, value| %>
    <div class="alert alert-<%= key %>"><%= value %></div>
  <% end %>
  <% if content_for?(:page_header) %>
    <div class="row">
      <div class="col-xs-12 main-header">
        <%= yield(:page_header) %>
      </div>
    </div>
  <% end %>

  <%= content_for?(:content) ? yield(:content) : yield %>
</div>

<%= render 'shared/footer' %>

</body>
</html>
